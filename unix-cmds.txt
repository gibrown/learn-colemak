estest mv cd estest mv cd estest mv cd
> ls which > ls which > ls which

--slack_report=@gibrown git diff --slack_report=@gibrown git diff --slack_report=@gibrown git diff
.. push | .. push | .. push |

--version=4 list svn --version=4 list svn --version=4 list svn
cp --name=global deploy cp --name=global deploy cp --name=global deploy

grep up ci grep up ci grep up ci
gs bulk-index wp gs bulk-index wp gs bulk-index wp

elasticsearch phpunit run elasticsearch phpunit run elasticsearch phpunit run
rm ssh sst rm ssh sst rm ssh sst

wpsb functions.php add wpsb functions.php add wpsb functions.php add
less sup hig less sup hig less sup hig

wpcom stash svndiff wpcom stash svndiff wpcom stash svndiff
sort * * sort * * sort * *

